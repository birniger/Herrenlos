.env
# herrenlos.db IS committed (it's the shared scan database).
# enum.db is tracked via Git LFS (see .gitattributes) — no longer gitignored.
*.db-shm
*.db-wal
__pycache__/
*.pyc
.DS_Store

# Skill manager local installs
.agents/
skills-lock.json
