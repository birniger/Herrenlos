requests>=2.31.0
beautifulsoup4>=4.12.0
playwright>=1.44.0
playwright-stealth>=1.0.6
lxml>=5.0.0
# UR CAPTCHA solver deps (all optional — scanner degrades gracefully without them)
cairosvg>=2.7.0
pillow>=10.0.0
pytesseract>=0.3.10
numpy>=1.24.0
anthropic>=0.101.0   # Claude vision fallback; needs ANTHROPIC_API_KEY env var
