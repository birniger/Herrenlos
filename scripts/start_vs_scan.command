#!/usr/bin/env bash
# Opened by terminal-notifier when the VS token-expired notification is tapped.
# Terminal.app auto-runs .command files in a new window.
cd "/Users/basilirniger/Local Docs/herrenlos_scanner"
exec python3 main.py vs
